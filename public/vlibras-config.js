// Configuração global para o VLibras
window.vwOptions = {
  // Define configurações específicas do VLibras
  position: 'R', // R = right (direita), L = left (esquerda)
  opacity: 1, // Opacidade do widget
  rootPath: 'https://vlibras.gov.br/app', // Caminho da raiz do VLibras
  wrapper: 'body', // Elemento onde o VLibras será inserido
  showAtStart: true, // Exibe o widget ao iniciar
  forceOnload: true // Força o carregamento ao iniciar
}; 